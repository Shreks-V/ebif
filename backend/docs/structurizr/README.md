# Structurizr

The main workspace lives in `workspace.dsl`.

Suggested usage:

```bash
structurizr validate -workspace workspace.dsl
structurizr export -workspace workspace.dsl -format plantuml
```

Or open it with Structurizr Lite and point it to this directory.
